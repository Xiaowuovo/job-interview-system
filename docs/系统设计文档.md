# 基于SpringBoot的就业面试辅助系统 - 设计文档

## 目录
- [1. 系统概述](#1-系统概述)
- [2. 核心模块设计](#2-核心模块设计)
- [3. 详细功能设计](#3-详细功能设计)
- [4. 数据库设计](#4-数据库设计)
- [5. API接口设计](#5-api接口设计)
- [6. 技术架构](#6-技术架构)

---

## 1. 系统概述

### 1.1 项目背景

随着就业竞争日益激烈，求职者需要系统化的面试准备工具。参考牛客网、LeetCode、拉勾网等主流平台，本系统聚焦于为求职者提供全方位的面试辅助服务。

### 1.2 系统定位

**核心价值**：打造一站式面试备考平台，涵盖知识学习、实战演练、智能评估、个性化推荐四大核心能力。

**目标用户**：
- 应届毕业生
- 求职转行者
- 在职提升者

### 1.3 核心竞争力

| 特性 | 说明 | 参考平台 |
|------|------|----------|
| 智能题库 | 分类、难度、标签多维度筛选 | LeetCode、牛客网 |
| AI模拟面试 | 真人对话式面试模拟 | HireVue、牛客网 |
| 知识图谱 | 结构化知识体系 | 拉勾教育 |
| 数据分析 | 个人能力雷达图、薄弱点分析 | BOSS直聘 |
| 社区互动 | 面经分享、问题讨论 | 牛客网、知乎 |

---

## 2. 核心模块设计

### 2.1 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        前端层 (Vue.js)                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │ 学习中心 │  │ 练习中心 │  │ 面试中心 │  │ 个人中心 │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │
└─────────────────────────────────────────────────────────────┘
                            ↕ RESTful API
┌─────────────────────────────────────────────────────────────┐
│                    应用层 (Spring Boot)                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │知识库模块│  │题库模块  │  │面试模块  │  │用户模块  │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │推荐引擎  │  │数据分析  │  │AI服务    │  │社区模块  │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │
└─────────────────────────────────────────────────────────────┘
                            ↕
┌─────────────────────────────────────────────────────────────┐
│                    数据层 (MySQL + Redis)                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │用户数据  │  │题库数据  │  │学习记录  │  │面试记录  │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 核心模块概览

> **图例说明**:
> - ✅ 已实现 - 功能已完成开发
> - 🚧 开发中 - 正在开发
> - ⏳ 待实现 - 计划开发
> - 📝 设计阶段 - 仅有设计文档

#### 模块一：知识库管理模块 📚 ✅ 已实现
**功能定位**：结构化面试知识体系

**核心功能**：
1. 知识图谱展示（技术栈、知识点关联）
2. 分类学习路径（Java、前端、算法、系统设计等）
3. 学习进度追踪
4. 知识点标注与笔记
5. 视频/文档/代码示例多媒体支持

**数据实体**：
- 知识分类（Category）
- 知识点（KnowledgePoint）
- 学习资源（Resource）
- 学习记录（StudyRecord）

---

#### 模块二：智能题库模块 💡 ✅ 已实现
**功能定位**：覆盖全面的面试题库系统

**核心功能**：
1. **题目分类管理**
   - 按技术栈分类（Java、Python、前端、数据库等）
   - 按题型分类（选择题、编程题、简答题、系统设计题）
   - 按难度分级（简单、中等、困难）
   - 按公司标签（阿里、腾讯、字节等）

2. **智能刷题系统**
   - 随机练习模式
   - 专项突破模式
   - 错题本功能
   - 收藏夹功能
   - 每日一题推荐

3. **题目详情**
   - 题目描述
   - 多个参考答案
   - 答案解析
   - 相关知识点链接
   - 用户讨论区
   - 通过率统计

4. **在线编程环境**（针对算法题）
   - 代码编辑器
   - 多语言支持
   - 实时运行
   - 测试用例验证
   - 代码提交记录

**数据实体**：
- 题目（Question）
- 题目标签（QuestionTag）
- 用户答题记录（AnswerRecord）
- 代码提交（CodeSubmission）
- 错题本（WrongQuestionBook）

---

#### 模块三：AI模拟面试模块 🎤 ✅ 已实现
**功能定位**：真实面试场景模拟

**核心功能**：
1. **面试类型选择**
   - 技术面试（算法、系统设计、项目经验）
   - 行为面试（STAR法则）
   - HR面试（职业规划、薪资谈判）

2. **智能面试官**
   - AI提问（基于岗位和简历）
   - 追问机制
   - 实时反馈
   - 语音/文字交互支持

3. **面试评估**
   - 回答完整性评分
   - 逻辑清晰度评分
   - 技术深度评分
   - 沟通表达评分
   - 综合评分与排名

4. **面试回放**
   - 完整对话记录
   - 关键问题高亮
   - 改进建议
   - 优秀回答参考

5. **真人模拟面试**（进阶功能）
   - 视频面试预约
   - 面试官匹配
   - 面试评价体系

**数据实体**：
- 面试会话（InterviewSession）
- 面试问题（InterviewQuestion）
- 面试评估（InterviewEvaluation）
- 面试反馈（InterviewFeedback）

---

#### 模块四：测评与分析模块 📊 ✅ 已实现
**功能定位**：数据驱动的能力提升

**核心功能**：
1. **能力测评**
   - 入学测评（了解基础水平）
   - 阶段测评（检验学习成果）
   - 模拟笔试（真实考试环境）

2. **数据分析**
   - 个人能力雷达图
   - 知识点掌握度分析
   - 薄弱环节识别
   - 学习时长统计
   - 练习频率分析

3. **智能推荐**
   - 个性化学习路径
   - 题目推荐算法
   - 知识点推荐
   - 相似用户对比

4. **学习报告**
   - 周报/月报生成
   - 成长曲线展示
   - 目标达成度分析

**数据实体**：
- 测评记录（AssessmentRecord）
- 能力模型（AbilityModel）
- 学习统计（StudyStatistics）
- 推荐记录（RecommendationLog）

---

#### 模块五：用户与权限模块 👤 ✅ 已实现
**功能定位**：用户体系与权限管理

**核心功能**：
1. **用户管理**
   - 注册/登录（手机、邮箱、第三方）
   - 个人信息管理
   - 简历管理
   - 目标岗位设置

2. **角色权限**
   - 学生（免费版、会员版）
   - 教师/面试官（内容创建、审核）
   - 管理员（系统管理）

3. **会员体系**
   - 免费用户（基础功能）
   - VIP会员（全部功能、无广告、优先服务）
   - 企业用户（团队协作、数据看板）

4. **积分体系**
   - 签到积分
   - 学习积分
   - 分享积分
   - 积分兑换

**数据实体**：
- 用户（User）
- 角色（Role）
- 权限（Permission）
- 会员记录（MembershipRecord）
- 积分记录（PointsRecord）

---

#### 模块五.5：教师端管理模块 👨‍🏫 ⏳ 待实现 (P1)
**功能定位**：教师专属内容管理与教学辅助平台

**核心功能**：

1. **题库内容管理**
   - **题目创建与编辑**
     - 支持多种题型（选择题、简答题、编程题、系统设计题）
     - 富文本编辑器（支持代码高亮、图片、LaTeX公式）
     - 题目标签管理（技术栈、难度、公司、知识点）
     - 批量导入题目（Excel/JSON格式）

   - **题目审核流程**
     - 教师提交 → 管理员审核 → 发布上线
     - 审核状态：草稿、待审核、已通过、已拒绝
     - 审核意见反馈与修改建议

   - **题目质量管理**
     - 查看题目统计数据（答题人数、正确率、平均用时）
     - 学生反馈收集（错误报告、内容建议）
     - 题目优化建议（基于数据分析）

   - **参考答案与解析管理**
     - 多版本答案支持
     - 详细解析编写
     - 相关知识点关联
     - 扩展阅读资料链接

2. **知识库内容管理**
   - **知识点创建**
     - Markdown编辑器
     - 知识点分类与标签
     - 难度级别设置
     - 前置知识点关联

   - **教程创建**
     - 系列教程组织
     - 视频上传与管理
     - 代码示例库
     - 配套练习题

   - **内容素材库**
     - 图片素材管理
     - 代码模板库
     - 常用图表库
     - 引用资料库

3. **学生管理与数据分析**
   - **学生列表管理**
     - 查看教师课程下的学生
     - 学生学习进度查看
     - 学生能力评估报告
     - 学生学习轨迹分析

   - **作业与考试管理**
     - 创建练习任务
     - 设置截止时间
     - 自动批改（选择题、编程题）
     - 手动批改（简答题、系统设计题）
     - 成绩导出

   - **数据统计看板**
     - 学生活跃度统计
     - 题目完成率统计
     - 知识点掌握度分析
     - 薄弱知识点识别
     - 学习趋势图表

4. **课程与班级管理**
   - **课程创建**
     - 课程基本信息（名称、描述、封面）
     - 课程章节规划
     - 知识点与题目关联
     - 学习路径设计

   - **班级管理**
     - 创建班级
     - 学生邀请码生成
     - 学生名单管理
     - 班级公告发布

   - **教学计划**
     - 每周学习计划制定
     - 重点难点标注
     - 阶段性测验安排
     - 学习进度监控

5. **互动与反馈**
   - **学生问题答疑**
     - 学生提问列表
     - 在线回答问题
     - 常见问题整理（FAQ）
     - 问答历史记录

   - **评论与讨论管理**
     - 查看题目评论
     - 回复学生讨论
     - 精华评论置顶
     - 不当内容删除

   - **教学反馈收集**
     - 学生满意度调查
     - 课程评价查看
     - 改进建议收集
     - 教学效果评估

6. **个人中心（教师专属）**
   - **我的内容**
     - 我创建的题目（按状态筛选）
     - 我创建的知识点
     - 我的教程列表
     - 内容数据统计

   - **我的课程**
     - 授课课程列表
     - 班级管理入口
     - 课程数据概览

   - **收益与荣誉**
     - 内容贡献度积分
     - 学生好评率
     - 优秀教师认证
     - 平台推荐次数

   - **教师认证**
     - 实名认证
     - 专业领域认证
     - 教学资质上传
     - 个人简介编辑

**数据实体**：
- 教师信息（TeacherProfile）
- 课程（Course）
- 班级（Class）
- 作业任务（Assignment）
- 批改记录（GradingRecord）
- 教学统计（TeachingStatistics）
- 内容审核（ContentReview）

**教师端权限控制**：
```java
// 教师可访问的API端点
/api/teacher/questions/**      // 题目管理
/api/teacher/knowledge/**      // 知识点管理
/api/teacher/courses/**        // 课程管理
/api/teacher/classes/**        // 班级管理
/api/teacher/assignments/**    // 作业管理
/api/teacher/students/**       // 学生管理
/api/teacher/statistics/**     // 数据统计
/api/teacher/profile/**        // 个人中心
```

**教师端核心流程**：

1. **题目发布流程**
```
创建题目 → 填写详情 → 添加标签 → 预览 → 提交审核 → 审核通过 → 发布上线
```

2. **课程创建流程**
```
创建课程 → 规划章节 → 关联内容 → 设计路径 → 创建班级 → 邀请学生 → 发布作业 → 批改作业 → 数据分析
```

3. **学生管理流程**
```
查看学生列表 → 分析学习数据 → 识别薄弱点 → 针对性辅导 → 发布练习 → 跟踪进度 → 效果评估
```

---

#### 模块六：社区互动模块 💬 ⏳ 待实现 (P2)
**功能定位**：知识分享与交流

**核心功能**：
1. **面经分享**
   - 面试经验发布
   - 公司面试流程
   - 面试题目分享
   - 面试技巧总结

2. **问答社区**
   - 技术问题提问
   - 专家解答
   - 答案投票
   - 最佳答案

3. **学习小组**
   - 创建学习小组
   - 组队刷题
   - 进度PK
   - 互相监督

4. **内容运营**
   - 热门话题推荐
   - 优质内容精选
   - 用户排行榜
   - 徽章系统

**数据实体**：
- 帖子（Post）
- 评论（Comment）
- 小组（StudyGroup）
- 点赞收藏（Like/Favorite）

---

#### 模块七：简历与求职模块 📄 ⏳ 待实现 (P2)
**功能定位**：简历优化与职位推荐

**核心功能**：
1. **简历管理**
   - 在线简历编辑器
   - 简历模板库
   - 简历导出（PDF、Word）
   - 简历评分

2. **简历优化**
   - AI简历诊断
   - 关键词优化建议
   - 项目经验润色
   - ATS友好度检测

3. **职位推荐**
   - 基于简历推荐职位
   - 基于学习记录推荐
   - 岗位匹配度分析
   - 投递记录管理

4. **求职助手**
   - 面试日程管理
   - 公司信息查询
   - 薪资水平参考
   - Offer比较工具

**数据实体**：
- 简历（Resume）
- 职位（Job）
- 投递记录（Application）
- 面试日程（InterviewSchedule）

---

## 3. 详细功能设计

### 3.1 智能题库详细设计

#### 3.1.1 题目数据结构

```java
@Entity
public class Question {
    @Id
    private Long id;

    // 基本信息
    private String title;              // 题目标题
    private String content;            // 题目描述（支持Markdown）
    private QuestionType type;         // 题型（选择、编程、简答、系统设计）
    private Difficulty difficulty;     // 难度

    // 分类标签
    @ManyToMany
    private Set<Tag> tags;            // 标签（Java、算法、数据库等）
    private String category;          // 一级分类
    private String subCategory;       // 二级分类

    // 公司相关
    @ManyToMany
    private Set<Company> companies;   // 出现的公司
    private Integer frequency;        // 出现频率

    // 题目内容（选择题）
    private String optionA;
    private String optionB;
    private String optionC;
    private String optionD;
    private String correctAnswer;

    // 题目内容（编程题）
    private String inputFormat;       // 输入格式
    private String outputFormat;      // 输出格式
    private String sampleInput;       // 示例输入
    private String sampleOutput;      // 示例输出
    private String timeLimit;         // 时间限制
    private String memoryLimit;       // 内存限制

    // 答案与解析
    @OneToMany
    private List<Answer> answers;     // 多个参考答案
    private String explanation;       // 详细解析
    private String keyPoints;         // 知识点

    // 统计数据
    private Integer viewCount;        // 浏览次数
    private Integer attemptCount;     // 尝试次数
    private Integer passCount;        // 通过次数
    private Double passRate;          // 通过率
    private Double avgScore;          // 平均分

    // 关联
    @ManyToMany
    private Set<KnowledgePoint> relatedKnowledge; // 关联知识点

    private LocalDateTime createdAt;
    private LocalDateTime updatedAt;
    private Long createdBy;           // 创建者
}
```

#### 3.1.2 刷题策略算法

**1. 智能推荐算法**

```java
public class QuestionRecommendationService {

    /**
     * 基于用户画像的题目推荐
     * 考虑因素：
     * 1. 用户目标岗位
     * 2. 历史答题记录
     * 3. 薄弱知识点
     * 4. 热门题目
     * 5. 遗忘曲线
     */
    public List<Question> recommendQuestions(Long userId, int count) {
        User user = userService.getById(userId);

        // 获取用户能力模型
        AbilityModel ability = getAbilityModel(userId);

        // 获取薄弱知识点
        List<KnowledgePoint> weakPoints = getWeakKnowledgePoints(ability);

        // 获取已做题目
        Set<Long> doneQuestions = getDoneQuestions(userId);

        // 推荐权重计算
        Map<Long, Double> scores = new HashMap<>();

        List<Question> candidates = questionRepository
            .findByTargetPosition(user.getTargetPosition());

        for (Question q : candidates) {
            if (doneQuestions.contains(q.getId())) continue;

            double score = 0.0;

            // 薄弱点权重（40%）
            if (isRelatedToWeakPoints(q, weakPoints)) {
                score += 0.4;
            }

            // 难度匹配（30%）
            score += getDifficultyScore(q, ability) * 0.3;

            // 热门度（20%）
            score += getPopularityScore(q) * 0.2;

            // 新鲜度（10%）
            score += getFreshnessScore(q) * 0.1;

            scores.put(q.getId(), score);
        }

        // 按分数排序返回
        return scores.entrySet().stream()
            .sorted(Map.Entry.<Long, Double>comparingByValue().reversed())
            .limit(count)
            .map(e -> questionRepository.findById(e.getKey()).get())
            .collect(Collectors.toList());
    }
}
```

**2. 错题本复习策略**

```java
/**
 * 基于艾宾浩斯遗忘曲线的复习提醒
 * 复习时间点：1天、2天、4天、7天、15天
 */
public class ReviewScheduler {

    private static final int[] REVIEW_INTERVALS = {1, 2, 4, 7, 15};

    public List<Question> getReviewQuestions(Long userId) {
        List<WrongQuestion> wrongQuestions =
            wrongQuestionRepository.findByUserId(userId);

        List<Question> needReview = new ArrayList<>();
        LocalDate today = LocalDate.now();

        for (WrongQuestion wq : wrongQuestions) {
            int reviewCount = wq.getReviewCount();

            if (reviewCount >= REVIEW_INTERVALS.length) {
                continue; // 已充分复习
            }

            LocalDate lastReview = wq.getLastReviewDate();
            long daysPassed = ChronoUnit.DAYS.between(lastReview, today);

            if (daysPassed >= REVIEW_INTERVALS[reviewCount]) {
                needReview.add(wq.getQuestion());
            }
        }

        return needReview;
    }
}
```

### 3.2 AI模拟面试详细设计

#### 3.2.1 面试流程设计

```
开始面试
    ↓
选择面试类型（技术/行为/HR）
    ↓
AI生成开场问题（基于岗位+简历）
    ↓
┌─────────────────┐
│  面试对话循环   │
│                 │
│ 1. AI提问       │
│ 2. 用户回答     │
│ 3. AI分析       │
│ 4. 追问/下一题  │
└─────────────────┘
    ↓
面试结束
    ↓
生成评估报告
    ↓
展示改进建议
```

#### 3.2.2 AI面试问题生成策略

```java
public class InterviewQuestionGenerator {

    /**
     * 根据岗位和简历生成面试问题
     */
    public List<InterviewQuestion> generateQuestions(
            String position, Resume resume, InterviewType type) {

        List<InterviewQuestion> questions = new ArrayList<>();

        switch (type) {
            case TECHNICAL:
                // 技术面试问题
                questions.addAll(generateTechnicalQuestions(position, resume));
                break;
            case BEHAVIORAL:
                // 行为面试问题（STAR法则）
                questions.addAll(generateBehavioralQuestions(resume));
                break;
            case HR:
                // HR面试问题
                questions.addAll(generateHRQuestions(position, resume));
                break;
        }

        return questions;
    }

    private List<InterviewQuestion> generateTechnicalQuestions(
            String position, Resume resume) {

        List<InterviewQuestion> questions = new ArrayList<>();

        // 1. 基础知识问题（30%）
        List<String> skills = resume.getSkills();
        for (String skill : skills.subList(0, Math.min(3, skills.size()))) {
            questions.add(InterviewQuestion.builder()
                .content("请介绍一下" + skill + "的核心特性和使用场景")
                .category("基础知识")
                .difficulty(Difficulty.EASY)
                .expectedKeyPoints(getKeyPoints(skill))
                .build());
        }

        // 2. 项目经验问题（40%）
        List<Project> projects = resume.getProjects();
        for (Project project : projects.subList(0, Math.min(2, projects.size()))) {
            questions.add(InterviewQuestion.builder()
                .content("请详细介绍" + project.getName() +
                        "项目，包括技术选型、架构设计、你的职责和遇到的挑战")
                .category("项目经验")
                .difficulty(Difficulty.MEDIUM)
                .relatedProject(project.getId())
                .build());
        }

        // 3. 算法与编程（20%）
        questions.add(InterviewQuestion.builder()
            .content("请实现一个LRU缓存算法")
            .category("算法编程")
            .difficulty(Difficulty.MEDIUM)
            .codeQuestion(true)
            .build());

        // 4. 系统设计（10%）
        questions.add(InterviewQuestion.builder()
            .content("设计一个高并发的秒杀系统")
            .category("系统设计")
            .difficulty(Difficulty.HARD)
            .build());

        return questions;
    }
}
```

#### 3.2.3 回答评分算法

```java
public class AnswerEvaluator {

    /**
     * 综合评分模型
     */
    public EvaluationResult evaluate(InterviewQuestion question,
                                     String userAnswer) {

        EvaluationResult result = new EvaluationResult();

        // 1. 完整性评分（30分）
        double completeness = evaluateCompleteness(
            question.getExpectedKeyPoints(), userAnswer);
        result.setCompletenessScore(completeness * 30);

        // 2. 准确性评分（40分）
        double accuracy = evaluateAccuracy(
            question.getStandardAnswer(), userAnswer);
        result.setAccuracyScore(accuracy * 40);

        // 3. 逻辑性评分（20分）
        double logic = evaluateLogic(userAnswer);
        result.setLogicScore(logic * 20);

        // 4. 表达能力（10分）
        double expression = evaluateExpression(userAnswer);
        result.setExpressionScore(expression * 10);

        // 总分
        result.setTotalScore(
            result.getCompletenessScore() +
            result.getAccuracyScore() +
            result.getLogicScore() +
            result.setExpressionScore()
        );

        // 生成反馈建议
        result.setFeedback(generateFeedback(result, question));

        return result;
    }

    /**
     * 完整性评估：检查是否覆盖关键知识点
     */
    private double evaluateCompleteness(
            List<String> keyPoints, String answer) {

        if (keyPoints == null || keyPoints.isEmpty()) {
            return 1.0;
        }

        int coveredCount = 0;
        for (String point : keyPoints) {
            if (answer.toLowerCase().contains(point.toLowerCase())) {
                coveredCount++;
            }
        }

        return (double) coveredCount / keyPoints.size();
    }

    /**
     * 准确性评估：使用NLP计算答案相似度
     */
    private double evaluateAccuracy(String standardAnswer, String userAnswer) {
        // 使用余弦相似度或BERT模型计算语义相似度
        // 这里简化处理
        return semanticSimilarity(standardAnswer, userAnswer);
    }
}
```

### 3.3 数据分析详细设计

#### 3.3.1 能力模型

```java
@Entity
public class AbilityModel {
    @Id
    private Long id;

    private Long userId;

    // 技术能力维度
    private Double algorithmAbility;      // 算法能力 (0-100)
    private Double codingAbility;         // 编码能力
    private Double systemDesignAbility;   // 系统设计
    private Double databaseAbility;       // 数据库
    private Double networkAbility;        // 计算机网络
    private Double osAbility;             // 操作系统

    // 语言框架能力
    @ElementCollection
    private Map<String, Double> languageSkills; // Java:85, Python:70

    @ElementCollection
    private Map<String, Double> frameworkSkills; // Spring:80, Vue:75

    // 综合能力
    private Double communicationAbility;  // 沟通表达
    private Double problemSolvingAbility; // 问题解决
    private Double learningAbility;       // 学习能力

    // 总体评分
    private Double overallScore;

    // 更新时间
    private LocalDateTime updatedAt;
}
```

#### 3.3.2 学习数据分析

```java
public class StudyAnalysisService {

    /**
     * 生成个人学习报告
     */
    public StudyReport generateReport(Long userId, ReportPeriod period) {

        StudyReport report = new StudyReport();

        // 1. 基础统计
        StudyStatistics stats = calculateStatistics(userId, period);
        report.setStatistics(stats);

        // 2. 能力雷达图
        AbilityModel ability = getAbilityModel(userId);
        report.setAbilityRadar(ability);

        // 3. 知识点掌握度
        Map<String, Double> knowledgeMastery =
            calculateKnowledgeMastery(userId);
        report.setKnowledgeMastery(knowledgeMastery);

        // 4. 薄弱环节
        List<WeakPoint> weakPoints = identifyWeakPoints(userId);
        report.setWeakPoints(weakPoints);

        // 5. 成长曲线
        List<GrowthPoint> growthCurve = getGrowthCurve(userId, period);
        report.setGrowthCurve(growthCurve);

        // 6. 学习建议
        List<String> suggestions = generateSuggestions(report);
        report.setSuggestions(suggestions);

        return report;
    }

    /**
     * 识别薄弱环节
     */
    private List<WeakPoint> identifyWeakPoints(Long userId) {
        List<WeakPoint> weakPoints = new ArrayList<>();

        // 分析答题记录
        List<AnswerRecord> records = answerRecordRepository
            .findByUserId(userId);

        // 按知识点分组统计
        Map<String, List<AnswerRecord>> groupedByKP =
            records.stream()
                .collect(Collectors.groupingBy(
                    r -> r.getQuestion().getKeyPoints()));

        for (Map.Entry<String, List<AnswerRecord>> entry :
                groupedByKP.entrySet()) {

            String knowledgePoint = entry.getKey();
            List<AnswerRecord> kpRecords = entry.getValue();

            // 计算正确率
            long correctCount = kpRecords.stream()
                .filter(AnswerRecord::isCorrect)
                .count();

            double accuracy = (double) correctCount / kpRecords.size();

            // 正确率低于60%视为薄弱点
            if (accuracy < 0.6) {
                weakPoints.add(WeakPoint.builder()
                    .knowledgePoint(knowledgePoint)
                    .accuracy(accuracy)
                    .attemptCount(kpRecords.size())
                    .priority(calculatePriority(accuracy, kpRecords.size()))
                    .build());
            }
        }

        // 按优先级排序
        weakPoints.sort(Comparator.comparing(WeakPoint::getPriority).reversed());

        return weakPoints;
    }
}
```

---

## 4. 数据库设计

### 4.1 核心表结构

#### 用户相关表 ✅ 已实现

```sql
-- 用户表 ✅ 已实现
CREATE TABLE `user` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `username` VARCHAR(50) UNIQUE NOT NULL COMMENT '用户名',
  `password` VARCHAR(100) NOT NULL COMMENT '密码（加密）',
  `email` VARCHAR(100) UNIQUE COMMENT '邮箱',
  `phone` VARCHAR(20) UNIQUE COMMENT '手机号',
  `avatar` VARCHAR(255) COMMENT '头像URL',
  `nickname` VARCHAR(50) COMMENT '昵称',
  `role` ENUM('STUDENT', 'TEACHER', 'ADMIN') DEFAULT 'STUDENT',
  `membership_type` ENUM('FREE', 'VIP', 'ENTERPRISE') DEFAULT 'FREE',
  `membership_expire_at` DATETIME COMMENT 'VIP到期时间',
  `target_position` VARCHAR(100) COMMENT '目标岗位',
  `points` INT DEFAULT 0 COMMENT '积分',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX `idx_email` (`email`),
  INDEX `idx_phone` (`phone`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- 用户简历表
CREATE TABLE `resume` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `name` VARCHAR(50) NOT NULL COMMENT '姓名',
  `education` VARCHAR(50) COMMENT '学历',
  `school` VARCHAR(100) COMMENT '学校',
  `major` VARCHAR(100) COMMENT '专业',
  `graduation_year` INT COMMENT '毕业年份',
  `work_years` INT COMMENT '工作年限',
  `skills` TEXT COMMENT '技能（JSON数组）',
  `self_introduction` TEXT COMMENT '自我介绍',
  `resume_url` VARCHAR(255) COMMENT '简历文件URL',
  `score` INT COMMENT 'AI评分',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='简历表';

-- 项目经验表
CREATE TABLE `project_experience` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `resume_id` BIGINT NOT NULL,
  `project_name` VARCHAR(100) NOT NULL,
  `project_description` TEXT,
  `role` VARCHAR(50) COMMENT '担任角色',
  `responsibilities` TEXT COMMENT '职责描述',
  `technologies` TEXT COMMENT '技术栈（JSON数组）',
  `achievements` TEXT COMMENT '项目成果',
  `start_date` DATE,
  `end_date` DATE,
  FOREIGN KEY (`resume_id`) REFERENCES `resume`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='项目经验表';
```

#### 知识库相关表 ✅ 已实现

```sql
-- 知识分类表 ⏳ 待实现 (可用category字段替代)
CREATE TABLE `knowledge_category` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `name` VARCHAR(100) NOT NULL COMMENT '分类名称',
  `parent_id` BIGINT COMMENT '父分类ID',
  `level` INT COMMENT '层级',
  `sort_order` INT COMMENT '排序',
  `icon` VARCHAR(255) COMMENT '图标',
  `description` TEXT COMMENT '描述',
  FOREIGN KEY (`parent_id`) REFERENCES `knowledge_category`(`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='知识分类表';

-- 知识点表 ✅ 已实现
CREATE TABLE `knowledge_point` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `category_id` BIGINT NOT NULL,
  `title` VARCHAR(200) NOT NULL COMMENT '知识点标题',
  `content` LONGTEXT COMMENT '内容（支持Markdown）',
  `difficulty` ENUM('EASY', 'MEDIUM', 'HARD'),
  `importance` INT COMMENT '重要度(1-5)',
  `tags` VARCHAR(500) COMMENT '标签（逗号分隔）',
  `prerequisite_points` TEXT COMMENT '前置知识点ID（JSON数组）',
  `related_points` TEXT COMMENT '相关知识点ID（JSON数组）',
  `view_count` INT DEFAULT 0,
  `like_count` INT DEFAULT 0,
  `created_by` BIGINT,
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (`category_id`) REFERENCES `knowledge_category`(`id`),
  INDEX `idx_category` (`category_id`),
  INDEX `idx_difficulty` (`difficulty`),
  FULLTEXT INDEX `ft_content` (`title`, `content`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='知识点表';

-- 学习资源表
CREATE TABLE `learning_resource` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `knowledge_point_id` BIGINT NOT NULL,
  `title` VARCHAR(200),
  `type` ENUM('VIDEO', 'DOCUMENT', 'CODE', 'LINK') NOT NULL,
  `url` VARCHAR(500) NOT NULL,
  `duration` INT COMMENT '时长（秒）',
  `file_size` BIGINT COMMENT '文件大小（字节）',
  `description` TEXT,
  FOREIGN KEY (`knowledge_point_id`) REFERENCES `knowledge_point`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学习资源表';

-- 学习记录表 ✅ 已实现
CREATE TABLE `study_record` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `knowledge_point_id` BIGINT NOT NULL,
  `progress` INT DEFAULT 0 COMMENT '进度（0-100）',
  `study_time` INT DEFAULT 0 COMMENT '学习时长（秒）',
  `status` ENUM('NOT_STARTED', 'IN_PROGRESS', 'COMPLETED') DEFAULT 'NOT_STARTED',
  `mastery_level` INT COMMENT '掌握度（0-100）',
  `notes` TEXT COMMENT '学习笔记',
  `last_study_at` DATETIME,
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`knowledge_point_id`) REFERENCES `knowledge_point`(`id`) ON DELETE CASCADE,
  UNIQUE KEY `uk_user_kp` (`user_id`, `knowledge_point_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学习记录表';
```

#### 题库相关表 ✅ 已实现

```sql
-- 题目表 ✅ 已实现
CREATE TABLE `question` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `title` VARCHAR(200) NOT NULL COMMENT '题目标题',
  `content` LONGTEXT NOT NULL COMMENT '题目内容',
  `type` ENUM('CHOICE', 'MULTIPLE_CHOICE', 'CODING', 'SHORT_ANSWER', 'SYSTEM_DESIGN') NOT NULL,
  `difficulty` ENUM('EASY', 'MEDIUM', 'HARD') NOT NULL,
  `category` VARCHAR(50) COMMENT '一级分类',
  `sub_category` VARCHAR(50) COMMENT '二级分类',
  `tags` VARCHAR(500) COMMENT '标签（逗号分隔）',

  -- 选择题字段
  `option_a` TEXT,
  `option_b` TEXT,
  `option_c` TEXT,
  `option_d` TEXT,
  `correct_answer` VARCHAR(10) COMMENT '正确答案（A/B/C/D或多选AB,CD）',

  -- 编程题字段
  `input_format` TEXT COMMENT '输入格式说明',
  `output_format` TEXT COMMENT '输出格式说明',
  `sample_input` TEXT COMMENT '样例输入',
  `sample_output` TEXT COMMENT '样例输出',
  `time_limit` INT COMMENT '时间限制（ms）',
  `memory_limit` INT COMMENT '内存限制（MB）',

  -- 通用字段
  `explanation` LONGTEXT COMMENT '答案解析',
  `key_points` VARCHAR(500) COMMENT '考察知识点',
  `reference_answer` LONGTEXT COMMENT '参考答案',

  -- 统计数据
  `view_count` INT DEFAULT 0,
  `attempt_count` INT DEFAULT 0,
  `pass_count` INT DEFAULT 0,
  `pass_rate` DECIMAL(5,2) COMMENT '通过率',
  `avg_score` DECIMAL(5,2) COMMENT '平均分',
  `like_count` INT DEFAULT 0,
  `collect_count` INT DEFAULT 0,

  `created_by` BIGINT,
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  INDEX `idx_type` (`type`),
  INDEX `idx_difficulty` (`difficulty`),
  INDEX `idx_category` (`category`),
  FULLTEXT INDEX `ft_title_content` (`title`, `content`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='题目表';

-- 题目标签表
CREATE TABLE `question_tag` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `question_id` BIGINT NOT NULL,
  `tag_name` VARCHAR(50) NOT NULL,
  `tag_type` ENUM('TECH', 'COMPANY', 'DIFFICULTY', 'CUSTOM'),
  FOREIGN KEY (`question_id`) REFERENCES `question`(`id`) ON DELETE CASCADE,
  INDEX `idx_tag` (`tag_name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='题目标签表';

-- 公司题库关联表
CREATE TABLE `question_company` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `question_id` BIGINT NOT NULL,
  `company_name` VARCHAR(100) NOT NULL,
  `frequency` INT DEFAULT 1 COMMENT '出现频率',
  `year` INT COMMENT '年份',
  FOREIGN KEY (`question_id`) REFERENCES `question`(`id`) ON DELETE CASCADE,
  INDEX `idx_company` (`company_name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='公司题库关联表';

-- 答题记录表 ✅ 已实现
CREATE TABLE `answer_record` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `question_id` BIGINT NOT NULL,
  `user_answer` TEXT COMMENT '用户答案',
  `is_correct` BOOLEAN COMMENT '是否正确',
  `score` DECIMAL(5,2) COMMENT '得分',
  `time_spent` INT COMMENT '用时（秒）',
  `submit_count` INT DEFAULT 1 COMMENT '提交次数',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`question_id`) REFERENCES `question`(`id`) ON DELETE CASCADE,
  INDEX `idx_user_question` (`user_id`, `question_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='答题记录表';

-- 代码提交表（编程题专用）
CREATE TABLE `code_submission` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `question_id` BIGINT NOT NULL,
  `language` VARCHAR(20) COMMENT '编程语言',
  `code` LONGTEXT NOT NULL COMMENT '提交代码',
  `status` ENUM('PENDING', 'RUNNING', 'ACCEPTED', 'WRONG_ANSWER', 'TIME_LIMIT', 'MEMORY_LIMIT', 'RUNTIME_ERROR', 'COMPILE_ERROR'),
  `pass_rate` DECIMAL(5,2) COMMENT '测试用例通过率',
  `execution_time` INT COMMENT '执行时间（ms）',
  `memory_used` INT COMMENT '内存使用（KB）',
  `error_message` TEXT COMMENT '错误信息',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`question_id`) REFERENCES `question`(`id`) ON DELETE CASCADE,
  INDEX `idx_user_question` (`user_id`, `question_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='代码提交表';

-- 错题本表 ✅ 已实现
CREATE TABLE `wrong_question_book` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `question_id` BIGINT NOT NULL,
  `wrong_count` INT DEFAULT 1 COMMENT '错误次数',
  `review_count` INT DEFAULT 0 COMMENT '复习次数',
  `last_review_at` DATETIME COMMENT '最后复习时间',
  `next_review_at` DATETIME COMMENT '下次复习时间',
  `is_mastered` BOOLEAN DEFAULT FALSE COMMENT '是否已掌握',
  `notes` TEXT COMMENT '笔记',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`question_id`) REFERENCES `question`(`id`) ON DELETE CASCADE,
  UNIQUE KEY `uk_user_question` (`user_id`, `question_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='错题本表';

-- 题目收藏表 ✅ 已实现
CREATE TABLE `question_favorites` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `question_id` BIGINT NOT NULL,
  `notes` TEXT COMMENT '收藏备注',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`question_id`) REFERENCES `question`(`id`) ON DELETE CASCADE,
  UNIQUE KEY `uk_user_question` (`user_id`, `question_id`),
  INDEX `idx_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='题目收藏表';

-- 每日一题表 ✅ 已实现
CREATE TABLE `daily_questions` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `date` DATE NOT NULL UNIQUE COMMENT '日期',
  `question_id` BIGINT NOT NULL,
  `category` VARCHAR(50) COMMENT '分类',
  `difficulty` VARCHAR(20) COMMENT '难度',
  `complete_count` INT DEFAULT 0 COMMENT '完成人数',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`question_id`) REFERENCES `question`(`id`) ON DELETE CASCADE,
  INDEX `idx_date` (`date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='每日一题表';

-- 积分记录表 ✅ 已实现
CREATE TABLE `points_records` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `points` INT NOT NULL COMMENT '积分变化（正数增加，负数减少）',
  `type` VARCHAR(50) NOT NULL COMMENT '积分类型：DAILY_SIGNIN, STUDY, PRACTICE, TEST, INTERVIEW, EXCHANGE',
  `description` VARCHAR(200) COMMENT '积分说明',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  INDEX `idx_user` (`user_id`),
  INDEX `idx_type` (`type`),
  INDEX `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='积分记录表';
```

#### 面试相关表 ✅ 已实现

```sql
-- 面试会话表 ✅ 已实现
CREATE TABLE `interview_session` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `session_type` ENUM('TECHNICAL', 'BEHAVIORAL', 'HR') NOT NULL,
  `position` VARCHAR(100) COMMENT '面试岗位',
  `resume_id` BIGINT COMMENT '使用的简历',
  `status` ENUM('ONGOING', 'COMPLETED', 'ABANDONED') DEFAULT 'ONGOING',
  `start_time` DATETIME NOT NULL,
  `end_time` DATETIME,
  `duration` INT COMMENT '时长（秒）',
  `total_score` DECIMAL(5,2) COMMENT '总分',
  `conversation` LONGTEXT COMMENT '完整对话记录（JSON）',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`resume_id`) REFERENCES `resume`(`id`),
  INDEX `idx_user_type` (`user_id`, `session_type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='面试会话表';

-- 面试问题表
CREATE TABLE `interview_question` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `session_id` BIGINT NOT NULL,
  `question_order` INT COMMENT '问题顺序',
  `question_type` ENUM('TECHNICAL', 'PROJECT', 'BEHAVIORAL', 'HR', 'FOLLOW_UP') NOT NULL,
  `question_content` TEXT NOT NULL,
  `expected_key_points` TEXT COMMENT '期望关键点（JSON数组）',
  `standard_answer` LONGTEXT COMMENT '标准答案',
  `difficulty` ENUM('EASY', 'MEDIUM', 'HARD'),
  `category` VARCHAR(50) COMMENT '问题分类',
  `asked_at` DATETIME,
  FOREIGN KEY (`session_id`) REFERENCES `interview_session`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='面试问题表';

-- 面试回答表
CREATE TABLE `interview_answer` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `question_id` BIGINT NOT NULL,
  `user_answer` LONGTEXT NOT NULL COMMENT '用户回答',
  `answer_time` INT COMMENT '回答用时（秒）',
  `is_satisfactory` BOOLEAN COMMENT 'AI判断是否满意',
  `triggered_follow_up` BOOLEAN DEFAULT FALSE COMMENT '是否触发追问',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`question_id`) REFERENCES `interview_question`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='面试回答表';

-- 面试评估表
CREATE TABLE `interview_evaluation` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `session_id` BIGINT NOT NULL,
  `question_id` BIGINT NOT NULL,
  `answer_id` BIGINT NOT NULL,

  -- 各维度评分
  `completeness_score` DECIMAL(5,2) COMMENT '完整性得分',
  `accuracy_score` DECIMAL(5,2) COMMENT '准确性得分',
  `logic_score` DECIMAL(5,2) COMMENT '逻辑性得分',
  `expression_score` DECIMAL(5,2) COMMENT '表达能力得分',
  `total_score` DECIMAL(5,2) COMMENT '总分',

  -- 详细反馈
  `strengths` TEXT COMMENT '优点',
  `weaknesses` TEXT COMMENT '不足',
  `suggestions` TEXT COMMENT '改进建议',
  `key_points_covered` TEXT COMMENT '覆盖的关键点（JSON）',
  `key_points_missed` TEXT COMMENT '遗漏的关键点（JSON）',

  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`session_id`) REFERENCES `interview_session`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`question_id`) REFERENCES `interview_question`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`answer_id`) REFERENCES `interview_answer`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='面试评估表';

-- 面试总结报告表
CREATE TABLE `interview_report` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `session_id` BIGINT NOT NULL,
  `overall_score` DECIMAL(5,2) COMMENT '综合得分',
  `ranking_percentile` DECIMAL(5,2) COMMENT '排名百分位',

  -- 能力维度得分
  `technical_score` DECIMAL(5,2) COMMENT '技术能力',
  `communication_score` DECIMAL(5,2) COMMENT '沟通能力',
  `problem_solving_score` DECIMAL(5,2) COMMENT '问题解决',
  `learning_ability_score` DECIMAL(5,2) COMMENT '学习能力',

  -- 详细分析
  `strengths_summary` TEXT COMMENT '优势总结',
  `weaknesses_summary` TEXT COMMENT '劣势总结',
  `improvement_plan` TEXT COMMENT '提升计划',
  `recommended_resources` TEXT COMMENT '推荐学习资源（JSON）',

  -- 对标分析
  `similar_users_avg_score` DECIMAL(5,2) COMMENT '相似用户平均分',
  `target_company_avg_score` DECIMAL(5,2) COMMENT '目标公司面试平均分',

  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`session_id`) REFERENCES `interview_session`(`id`) ON DELETE CASCADE,
  UNIQUE KEY `uk_session` (`session_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='面试报告表';
```

#### 数据分析相关表

```sql
-- 用户能力模型表 ✅ 已实现
CREATE TABLE `user_ability_model` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,

  -- 基础技术能力
  `algorithm_ability` DECIMAL(5,2) DEFAULT 0 COMMENT '算法能力',
  `coding_ability` DECIMAL(5,2) DEFAULT 0 COMMENT '编码能力',
  `system_design_ability` DECIMAL(5,2) DEFAULT 0 COMMENT '系统设计',
  `database_ability` DECIMAL(5,2) DEFAULT 0 COMMENT '数据库',
  `network_ability` DECIMAL(5,2) DEFAULT 0 COMMENT '计算机网络',
  `os_ability` DECIMAL(5,2) DEFAULT 0 COMMENT '操作系统',

  -- 语言技能（JSON格式：{"Java": 85, "Python": 70}）
  `language_skills` TEXT COMMENT '编程语言技能',
  `framework_skills` TEXT COMMENT '框架技能',

  -- 软技能
  `communication_ability` DECIMAL(5,2) DEFAULT 0,
  `teamwork_ability` DECIMAL(5,2) DEFAULT 0,
  `problem_solving_ability` DECIMAL(5,2) DEFAULT 0,
  `learning_ability` DECIMAL(5,2) DEFAULT 0,

  -- 综合评分
  `overall_score` DECIMAL(5,2) DEFAULT 0,
  `level` ENUM('BEGINNER', 'INTERMEDIATE', 'ADVANCED', 'EXPERT') DEFAULT 'BEGINNER',

  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  UNIQUE KEY `uk_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户能力模型表';

-- 学习统计表
CREATE TABLE `study_statistics` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `date` DATE NOT NULL COMMENT '统计日期',

  -- 学习数据
  `study_duration` INT DEFAULT 0 COMMENT '学习时长（分钟）',
  `knowledge_points_learned` INT DEFAULT 0 COMMENT '学习知识点数',
  `questions_attempted` INT DEFAULT 0 COMMENT '练习题目数',
  `questions_correct` INT DEFAULT 0 COMMENT '正确题目数',
  `accuracy_rate` DECIMAL(5,2) COMMENT '正确率',

  -- 活跃数据
  `login_count` INT DEFAULT 0 COMMENT '登录次数',
  `active_days` INT DEFAULT 0 COMMENT '活跃天数',
  `continuous_days` INT DEFAULT 0 COMMENT '连续学习天数',

  -- 成就数据
  `points_earned` INT DEFAULT 0 COMMENT '获得积分',
  `badges_earned` INT DEFAULT 0 COMMENT '获得徽章数',

  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  UNIQUE KEY `uk_user_date` (`user_id`, `date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学习统计表';

-- 知识点掌握度表
CREATE TABLE `knowledge_mastery` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `knowledge_point_id` BIGINT NOT NULL,
  `mastery_level` DECIMAL(5,2) DEFAULT 0 COMMENT '掌握度（0-100）',
  `practice_count` INT DEFAULT 0 COMMENT '练习次数',
  `correct_count` INT DEFAULT 0 COMMENT '正确次数',
  `accuracy_rate` DECIMAL(5,2) COMMENT '正确率',
  `last_practice_at` DATETIME COMMENT '最后练习时间',
  `status` ENUM('NOT_LEARNED', 'LEARNING', 'MASTERED') DEFAULT 'NOT_LEARNED',
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`knowledge_point_id`) REFERENCES `knowledge_point`(`id`) ON DELETE CASCADE,
  UNIQUE KEY `uk_user_kp` (`user_id`, `knowledge_point_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='知识点掌握度表';
```

#### 教师端相关表 ⏳ 待实现 (P1)

```sql
-- 教师信息扩展表 ⏳ 待实现
CREATE TABLE `teacher_profile` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL UNIQUE COMMENT '用户ID',
  `real_name` VARCHAR(50) COMMENT '真实姓名',
  `certification_status` ENUM('PENDING', 'VERIFIED', 'REJECTED') DEFAULT 'PENDING' COMMENT '认证状态',
  `certification_materials` TEXT COMMENT '认证材料URL（JSON数组）',
  `expertise_areas` TEXT COMMENT '专业领域（JSON数组）',
  `teaching_years` INT COMMENT '教学年限',
  `institution` VARCHAR(200) COMMENT '所在机构',
  `job_title` VARCHAR(100) COMMENT '职称',
  `introduction` TEXT COMMENT '个人简介',
  `honor_score` INT DEFAULT 0 COMMENT '荣誉积分',
  `student_count` INT DEFAULT 0 COMMENT '教授学生数',
  `course_count` INT DEFAULT 0 COMMENT '课程数量',
  `rating` DECIMAL(3,2) DEFAULT 5.00 COMMENT '教师评分',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  INDEX `idx_certification_status` (`certification_status`),
  INDEX `idx_rating` (`rating`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='教师信息表';

-- 课程表
CREATE TABLE `course` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `teacher_id` BIGINT NOT NULL COMMENT '教师ID',
  `title` VARCHAR(200) NOT NULL COMMENT '课程标题',
  `description` TEXT COMMENT '课程描述',
  `cover_image` VARCHAR(255) COMMENT '课程封面',
  `category` VARCHAR(100) COMMENT '课程分类',
  `difficulty` ENUM('BEGINNER', 'INTERMEDIATE', 'ADVANCED') COMMENT '难度',
  `tags` VARCHAR(500) COMMENT '标签（逗号分隔）',
  `learning_path` LONGTEXT COMMENT '学习路径（JSON）',
  `status` ENUM('DRAFT', 'PUBLISHED', 'ARCHIVED') DEFAULT 'DRAFT',
  `student_count` INT DEFAULT 0 COMMENT '学生人数',
  `rating` DECIMAL(3,2) DEFAULT 5.00 COMMENT '课程评分',
  `view_count` INT DEFAULT 0 COMMENT '浏览次数',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (`teacher_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  INDEX `idx_teacher` (`teacher_id`),
  INDEX `idx_category` (`category`),
  INDEX `idx_status` (`status`),
  FULLTEXT INDEX `ft_title_desc` (`title`, `description`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='课程表';

-- 课程章节表
CREATE TABLE `course_chapter` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `course_id` BIGINT NOT NULL,
  `title` VARCHAR(200) NOT NULL COMMENT '章节标题',
  `description` TEXT COMMENT '章节描述',
  `sort_order` INT NOT NULL COMMENT '排序',
  `parent_id` BIGINT COMMENT '父章节ID',
  `is_free` BOOLEAN DEFAULT FALSE COMMENT '是否免费',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`course_id`) REFERENCES `course`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`parent_id`) REFERENCES `course_chapter`(`id`) ON DELETE CASCADE,
  INDEX `idx_course` (`course_id`),
  INDEX `idx_sort` (`sort_order`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='课程章节表';

-- 课程内容关联表
CREATE TABLE `course_content` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `course_id` BIGINT NOT NULL,
  `chapter_id` BIGINT COMMENT '章节ID',
  `content_type` ENUM('KNOWLEDGE_POINT', 'QUESTION', 'TUTORIAL', 'VIDEO') NOT NULL,
  `content_id` BIGINT NOT NULL COMMENT '内容ID',
  `sort_order` INT NOT NULL COMMENT '排序',
  `is_required` BOOLEAN DEFAULT TRUE COMMENT '是否必修',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`course_id`) REFERENCES `course`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`chapter_id`) REFERENCES `course_chapter`(`id`) ON DELETE CASCADE,
  INDEX `idx_course_chapter` (`course_id`, `chapter_id`),
  INDEX `idx_content` (`content_type`, `content_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='课程内容关联表';

-- 班级表
CREATE TABLE `class` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `teacher_id` BIGINT NOT NULL COMMENT '教师ID',
  `course_id` BIGINT COMMENT '关联课程ID',
  `name` VARCHAR(100) NOT NULL COMMENT '班级名称',
  `description` TEXT COMMENT '班级描述',
  `invite_code` VARCHAR(20) UNIQUE NOT NULL COMMENT '邀请码',
  `max_students` INT DEFAULT 100 COMMENT '最大学生数',
  `student_count` INT DEFAULT 0 COMMENT '当前学生数',
  `start_date` DATE COMMENT '开班日期',
  `end_date` DATE COMMENT '结束日期',
  `status` ENUM('ACTIVE', 'CLOSED', 'ARCHIVED') DEFAULT 'ACTIVE',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (`teacher_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`course_id`) REFERENCES `course`(`id`) ON DELETE SET NULL,
  INDEX `idx_teacher` (`teacher_id`),
  INDEX `idx_invite_code` (`invite_code`),
  INDEX `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='班级表';

-- 班级成员表
CREATE TABLE `class_member` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `class_id` BIGINT NOT NULL,
  `user_id` BIGINT NOT NULL,
  `role` ENUM('STUDENT', 'ASSISTANT') DEFAULT 'STUDENT',
  `join_date` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `status` ENUM('ACTIVE', 'QUIT') DEFAULT 'ACTIVE',
  UNIQUE KEY `uk_class_user` (`class_id`, `user_id`),
  FOREIGN KEY (`class_id`) REFERENCES `class`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  INDEX `idx_class` (`class_id`),
  INDEX `idx_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='班级成员表';

-- 作业任务表
CREATE TABLE `assignment` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `teacher_id` BIGINT NOT NULL COMMENT '教师ID',
  `class_id` BIGINT COMMENT '班级ID',
  `course_id` BIGINT COMMENT '课程ID',
  `title` VARCHAR(200) NOT NULL COMMENT '作业标题',
  `description` TEXT COMMENT '作业描述',
  `type` ENUM('PRACTICE', 'EXAM', 'PROJECT') DEFAULT 'PRACTICE',
  `question_ids` TEXT COMMENT '题目ID列表（JSON数组）',
  `total_score` INT DEFAULT 100 COMMENT '总分',
  `passing_score` INT DEFAULT 60 COMMENT '及格分',
  `time_limit` INT COMMENT '时间限制（分钟）',
  `start_time` DATETIME COMMENT '开始时间',
  `deadline` DATETIME COMMENT '截止时间',
  `allow_late_submit` BOOLEAN DEFAULT FALSE COMMENT '允许延迟提交',
  `auto_grade` BOOLEAN DEFAULT TRUE COMMENT '自动批改',
  `status` ENUM('DRAFT', 'PUBLISHED', 'CLOSED') DEFAULT 'DRAFT',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (`teacher_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`class_id`) REFERENCES `class`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`course_id`) REFERENCES `course`(`id`) ON DELETE SET NULL,
  INDEX `idx_teacher` (`teacher_id`),
  INDEX `idx_class` (`class_id`),
  INDEX `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='作业任务表';

-- 作业提交表
CREATE TABLE `assignment_submission` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `assignment_id` BIGINT NOT NULL,
  `user_id` BIGINT NOT NULL,
  `answers` LONGTEXT COMMENT '答案（JSON格式）',
  `score` DECIMAL(5,2) COMMENT '得分',
  `auto_score` DECIMAL(5,2) COMMENT '自动评分',
  `manual_score` DECIMAL(5,2) COMMENT '人工评分',
  `status` ENUM('SUBMITTED', 'GRADING', 'GRADED') DEFAULT 'SUBMITTED',
  `submit_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `grade_time` DATETIME COMMENT '批改时间',
  `is_late` BOOLEAN DEFAULT FALSE COMMENT '是否延迟提交',
  `time_spent` INT COMMENT '用时（分钟）',
  UNIQUE KEY `uk_assignment_user` (`assignment_id`, `user_id`),
  FOREIGN KEY (`assignment_id`) REFERENCES `assignment`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  INDEX `idx_assignment` (`assignment_id`),
  INDEX `idx_user` (`user_id`),
  INDEX `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='作业提交表';

-- 批改记录表
CREATE TABLE `grading_record` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `submission_id` BIGINT NOT NULL,
  `question_id` BIGINT NOT NULL,
  `user_answer` TEXT COMMENT '学生答案',
  `is_correct` BOOLEAN COMMENT '是否正确',
  `score` DECIMAL(5,2) COMMENT '得分',
  `teacher_comment` TEXT COMMENT '教师评语',
  `graded_by` BIGINT COMMENT '批改教师ID',
  `graded_at` DATETIME COMMENT '批改时间',
  FOREIGN KEY (`submission_id`) REFERENCES `assignment_submission`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`question_id`) REFERENCES `question`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`graded_by`) REFERENCES `user`(`id`) ON DELETE SET NULL,
  INDEX `idx_submission` (`submission_id`),
  INDEX `idx_question` (`question_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='批改记录表';

-- 内容审核表
CREATE TABLE `content_review` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `content_type` ENUM('QUESTION', 'KNOWLEDGE_POINT', 'TUTORIAL', 'COURSE') NOT NULL,
  `content_id` BIGINT NOT NULL COMMENT '内容ID',
  `creator_id` BIGINT NOT NULL COMMENT '创建者ID',
  `reviewer_id` BIGINT COMMENT '审核者ID',
  `status` ENUM('PENDING', 'APPROVED', 'REJECTED', 'NEED_REVISION') DEFAULT 'PENDING',
  `review_comment` TEXT COMMENT '审核意见',
  `submit_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `review_time` DATETIME COMMENT '审核时间',
  `revision_count` INT DEFAULT 0 COMMENT '修改次数',
  FOREIGN KEY (`creator_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`reviewer_id`) REFERENCES `user`(`id`) ON DELETE SET NULL,
  INDEX `idx_content` (`content_type`, `content_id`),
  INDEX `idx_status` (`status`),
  INDEX `idx_creator` (`creator_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='内容审核表';

-- 教学统计表
CREATE TABLE `teaching_statistics` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `teacher_id` BIGINT NOT NULL,
  `stat_date` DATE NOT NULL COMMENT '统计日期',
  `total_students` INT DEFAULT 0 COMMENT '总学生数',
  `active_students` INT DEFAULT 0 COMMENT '活跃学生数',
  `questions_created` INT DEFAULT 0 COMMENT '创建题目数',
  `courses_created` INT DEFAULT 0 COMMENT '创建课程数',
  `assignments_created` INT DEFAULT 0 COMMENT '布置作业数',
  `assignments_graded` INT DEFAULT 0 COMMENT '批改作业数',
  `student_avg_score` DECIMAL(5,2) COMMENT '学生平均分',
  `positive_feedback_count` INT DEFAULT 0 COMMENT '好评数',
  `total_feedback_count` INT DEFAULT 0 COMMENT '总评价数',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY `uk_teacher_date` (`teacher_id`, `stat_date`),
  FOREIGN KEY (`teacher_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  INDEX `idx_teacher` (`teacher_id`),
  INDEX `idx_date` (`stat_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='教学统计表';

-- 教师答疑表
CREATE TABLE `teacher_qa` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `student_id` BIGINT NOT NULL,
  `teacher_id` BIGINT COMMENT '指定教师',
  `question_content` TEXT NOT NULL COMMENT '问题内容',
  `question_images` TEXT COMMENT '问题图片（JSON数组）',
  `related_question_id` BIGINT COMMENT '相关题目ID',
  `related_knowledge_id` BIGINT COMMENT '相关知识点ID',
  `answer_content` TEXT COMMENT '回答内容',
  `answered_by` BIGINT COMMENT '回答教师ID',
  `status` ENUM('PENDING', 'ANSWERED', 'CLOSED') DEFAULT 'PENDING',
  `priority` ENUM('LOW', 'NORMAL', 'HIGH', 'URGENT') DEFAULT 'NORMAL',
  `is_public` BOOLEAN DEFAULT TRUE COMMENT '是否公开',
  `helpful_count` INT DEFAULT 0 COMMENT '有帮助数',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `answered_at` DATETIME COMMENT '回答时间',
  FOREIGN KEY (`student_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`teacher_id`) REFERENCES `user`(`id`) ON DELETE SET NULL,
  FOREIGN KEY (`answered_by`) REFERENCES `user`(`id`) ON DELETE SET NULL,
  INDEX `idx_student` (`student_id`),
  INDEX `idx_teacher` (`teacher_id`),
  INDEX `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='教师答疑表';

-- 课程评价表
CREATE TABLE `course_rating` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `course_id` BIGINT NOT NULL,
  `user_id` BIGINT NOT NULL,
  `rating` INT NOT NULL COMMENT '评分（1-5）',
  `content_quality` INT COMMENT '内容质量（1-5）',
  `teaching_method` INT COMMENT '教学方法（1-5）',
  `practice_quality` INT COMMENT '练习质量（1-5）',
  `comment` TEXT COMMENT '评价内容',
  `is_anonymous` BOOLEAN DEFAULT FALSE COMMENT '是否匿名',
  `is_verified` BOOLEAN DEFAULT FALSE COMMENT '是否已认证（完成课程）',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY `uk_course_user` (`course_id`, `user_id`),
  FOREIGN KEY (`course_id`) REFERENCES `course`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  INDEX `idx_course` (`course_id`),
  INDEX `idx_rating` (`rating`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='课程评价表';
```

#### 社区相关表 ⏳ 待实现 (P2)

```sql
-- 帖子表 ⏳ 待实现
CREATE TABLE `post` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `type` ENUM('INTERVIEW_EXPERIENCE', 'QUESTION', 'ARTICLE', 'DISCUSSION') NOT NULL,
  `title` VARCHAR(200) NOT NULL,
  `content` LONGTEXT NOT NULL,
  `tags` VARCHAR(500) COMMENT '标签（逗号分隔）',
  `company` VARCHAR(100) COMMENT '相关公司',
  `position` VARCHAR(100) COMMENT '相关职位',
  `is_top` BOOLEAN DEFAULT FALSE COMMENT '是否置顶',
  `is_essence` BOOLEAN DEFAULT FALSE COMMENT '是否精华',
  `view_count` INT DEFAULT 0,
  `like_count` INT DEFAULT 0,
  `comment_count` INT DEFAULT 0,
  `collect_count` INT DEFAULT 0,
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  INDEX `idx_type` (`type`),
  INDEX `idx_company` (`company`),
  FULLTEXT INDEX `ft_title_content` (`title`, `content`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='帖子表';

-- 评论表
CREATE TABLE `comment` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `post_id` BIGINT NOT NULL,
  `user_id` BIGINT NOT NULL,
  `parent_id` BIGINT COMMENT '父评论ID',
  `content` TEXT NOT NULL,
  `like_count` INT DEFAULT 0,
  `is_author_reply` BOOLEAN DEFAULT FALSE COMMENT '是否作者回复',
  `is_best_answer` BOOLEAN DEFAULT FALSE COMMENT '是否最佳答案',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`post_id`) REFERENCES `post`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`parent_id`) REFERENCES `comment`(`id`) ON DELETE CASCADE,
  INDEX `idx_post` (`post_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='评论表';

-- 学习小组表
CREATE TABLE `study_group` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `name` VARCHAR(100) NOT NULL,
  `description` TEXT,
  `avatar` VARCHAR(255),
  `creator_id` BIGINT NOT NULL,
  `member_count` INT DEFAULT 1,
  `max_member_count` INT DEFAULT 100,
  `target` VARCHAR(200) COMMENT '学习目标',
  `is_public` BOOLEAN DEFAULT TRUE,
  `status` ENUM('ACTIVE', 'INACTIVE') DEFAULT 'ACTIVE',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`creator_id`) REFERENCES `user`(`id`),
  INDEX `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学习小组表';

-- 小组成员表
CREATE TABLE `group_member` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `group_id` BIGINT NOT NULL,
  `user_id` BIGINT NOT NULL,
  `role` ENUM('OWNER', 'ADMIN', 'MEMBER') DEFAULT 'MEMBER',
  `join_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (`group_id`) REFERENCES `study_group`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`id`) ON DELETE CASCADE,
  UNIQUE KEY `uk_group_user` (`group_id`, `user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='小组成员表';
```

---

## 5. API接口设计

### 5.1 接口规范

#### 通用响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "timestamp": 1702789200000
}
```

#### 错误码定义

| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器错误 |
| 1001 | 用户名已存在 |
| 1002 | 用户名或密码错误 |
| 2001 | 题目不存在 |
| 3001 | 会员已过期 |

### 5.2 核心接口文档

#### 用户模块

```
POST   /api/users/register          注册
POST   /api/users/login             登录
GET    /api/users/profile           获取个人信息
PUT    /api/users/profile           更新个人信息
POST   /api/users/logout            登出
GET    /api/users/ability-model     获取能力模型
```

#### 知识库模块

```
GET    /api/knowledge/categories              获取知识分类树
GET    /api/knowledge/points                  获取知识点列表
GET    /api/knowledge/points/{id}             获取知识点详情
POST   /api/knowledge/points/{id}/study       记录学习
GET    /api/knowledge/my-progress             我的学习进度
GET    /api/knowledge/recommend               推荐知识点
```

#### 题库模块

```
GET    /api/questions                         获取题目列表
GET    /api/questions/{id}                    获取题目详情
GET    /api/questions/random                  随机题目
POST   /api/questions/{id}/submit             提交答案
GET    /api/questions/wrong-book              我的错题本
POST   /api/questions/{id}/collect            收藏题目
GET    /api/questions/recommend               推荐题目
GET    /api/questions/daily                   每日一题
```

#### 面试模块

```
POST   /api/interview/sessions                创建面试会话
GET    /api/interview/sessions/{id}           获取会话详情
POST   /api/interview/sessions/{id}/chat      AI对话
POST   /api/interview/sessions/{id}/end       结束面试
GET    /api/interview/sessions/{id}/report    获取面试报告
GET    /api/interview/my-sessions             我的面试记录
```

#### 数据分析模块

```
GET    /api/analysis/overview                 数据概览
GET    /api/analysis/report/daily             日报
GET    /api/analysis/report/weekly            周报
GET    /api/analysis/report/monthly           月报
GET    /api/analysis/weak-points              薄弱点分析
GET    /api/analysis/growth-curve             成长曲线
GET    /api/analysis/ranking                  排名情况
```

#### 教师端模块

```
# 教师信息管理
GET    /api/teacher/profile                   获取教师资料
PUT    /api/teacher/profile                   更新教师资料
POST   /api/teacher/certification             提交认证申请
GET    /api/teacher/statistics                获取教学统计

# 题目管理
GET    /api/teacher/questions                 我的题目列表
GET    /api/teacher/questions/{id}            题目详情
POST   /api/teacher/questions                 创建题目
PUT    /api/teacher/questions/{id}            更新题目
DELETE /api/teacher/questions/{id}            删除题目
POST   /api/teacher/questions/batch-import    批量导入题目
POST   /api/teacher/questions/{id}/submit-review  提交审核
GET    /api/teacher/questions/{id}/statistics 题目统计数据

# 知识点管理
GET    /api/teacher/knowledge-points          我的知识点列表
POST   /api/teacher/knowledge-points          创建知识点
PUT    /api/teacher/knowledge-points/{id}     更新知识点
DELETE /api/teacher/knowledge-points/{id}     删除知识点
POST   /api/teacher/knowledge-points/{id}/submit-review  提交审核

# 教程管理
GET    /api/teacher/tutorials                 我的教程列表
POST   /api/teacher/tutorials                 创建教程
PUT    /api/teacher/tutorials/{id}            更新教程
DELETE /api/teacher/tutorials/{id}            删除教程
POST   /api/teacher/tutorials/{id}/upload-video  上传视频

# 课程管理
GET    /api/teacher/courses                   我的课程列表
GET    /api/teacher/courses/{id}              课程详情
POST   /api/teacher/courses                   创建课程
PUT    /api/teacher/courses/{id}              更新课程
DELETE /api/teacher/courses/{id}              删除课程
POST   /api/teacher/courses/{id}/publish      发布课程
POST   /api/teacher/courses/{id}/chapters     添加章节
PUT    /api/teacher/courses/{courseId}/chapters/{id}  更新章节
DELETE /api/teacher/courses/{courseId}/chapters/{id}  删除章节
POST   /api/teacher/courses/{id}/add-content  添加课程内容
GET    /api/teacher/courses/{id}/statistics   课程统计数据
GET    /api/teacher/courses/{id}/ratings      课程评价列表

# 班级管理
GET    /api/teacher/classes                   我的班级列表
GET    /api/teacher/classes/{id}              班级详情
POST   /api/teacher/classes                   创建班级
PUT    /api/teacher/classes/{id}              更新班级
DELETE /api/teacher/classes/{id}              删除班级
POST   /api/teacher/classes/{id}/invite-code  生成邀请码
GET    /api/teacher/classes/{id}/students     班级学生列表
DELETE /api/teacher/classes/{id}/students/{userId}  移除学生
POST   /api/teacher/classes/{id}/announcement 发布公告

# 作业管理
GET    /api/teacher/assignments               我的作业列表
GET    /api/teacher/assignments/{id}          作业详情
POST   /api/teacher/assignments               创建作业
PUT    /api/teacher/assignments/{id}          更新作业
DELETE /api/teacher/assignments/{id}          删除作业
POST   /api/teacher/assignments/{id}/publish  发布作业
GET    /api/teacher/assignments/{id}/submissions  作业提交列表
GET    /api/teacher/assignments/{id}/submissions/{submissionId}  提交详情
POST   /api/teacher/assignments/submissions/{id}/grade  批改作业
GET    /api/teacher/assignments/{id}/statistics  作业统计

# 学生管理
GET    /api/teacher/students                  我的学生列表
GET    /api/teacher/students/{id}/profile     学生详情
GET    /api/teacher/students/{id}/progress    学生学习进度
GET    /api/teacher/students/{id}/ability     学生能力评估
GET    /api/teacher/students/{id}/records     学生学习记录
GET    /api/teacher/students/statistics       学生统计分析

# 答疑管理
GET    /api/teacher/qa                        问题列表
GET    /api/teacher/qa/{id}                   问题详情
POST   /api/teacher/qa/{id}/answer            回答问题
PUT    /api/teacher/qa/{id}/answer            更新回答
POST   /api/teacher/qa/{id}/close             关闭问题
GET    /api/teacher/qa/statistics             答疑统计

# 内容审核
GET    /api/teacher/reviews                   我的审核记录
GET    /api/teacher/reviews/{id}              审核详情

# 数据分析
GET    /api/teacher/analytics/overview        数据总览
GET    /api/teacher/analytics/students-active 学生活跃度
GET    /api/teacher/analytics/questions-quality  题目质量分析
GET    /api/teacher/analytics/courses-performance  课程表现
GET    /api/teacher/analytics/teaching-effectiveness  教学效果
```

#### 社区模块

```
GET    /api/posts                             帖子列表
GET    /api/posts/{id}                        帖子详情
POST   /api/posts                             发布帖子
PUT    /api/posts/{id}                        更新帖子
DELETE /api/posts/{id}                        删除帖子
POST   /api/posts/{id}/comments               发表评论
GET    /api/posts/{id}/comments               获取评论列表
POST   /api/posts/{id}/like                   点赞
```

---

## 6. 技术架构

### 6.1 技术选型

#### 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 2.7.x | 核心框架 |
| Spring Security | 2.7.x | 安全认证 |
| Spring Data JPA | 2.7.x | 数据访问 |
| MySQL | 8.0 | 主数据库 |
| Redis | 7.0 | 缓存/会话 |
| Elasticsearch | 7.x | 全文搜索 |
| RabbitMQ | 3.x | 消息队列 |
| MinIO | - | 对象存储 |

#### 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 3.x | 前端框架 |
| Vue Router | 4.x | 路由管理 |
| Pinia | 2.x | 状态管理 |
| Element Plus | 2.x | UI组件库 |
| ECharts | 5.x | 数据可视化 |
| Axios | 1.x | HTTP客户端 |
| Markdown-it | - | Markdown渲染 |

#### AI服务

| 服务 | 用途 |
|------|------|
| OpenAI GPT | AI面试对话 |
| 百度文心一言 | 备用AI服务 |
| HuggingFace | NLP模型 |

### 6.2 系统架构图

```
                        ┌─────────────┐
                        │  Nginx      │
                        │  (反向代理)  │
                        └─────────────┘
                              │
                ┌─────────────┴─────────────┐
                │                           │
         ┌──────▼──────┐            ┌──────▼──────┐
         │   前端静态   │            │  API Gateway │
         │   资源服务   │            │              │
         └─────────────┘            └──────┬───────┘
                                           │
                        ┌──────────────────┼──────────────────┐
                        │                  │                  │
                 ┌──────▼──────┐   ┌──────▼──────┐   ┌──────▼──────┐
                 │  用户服务    │   │  题库服务    │   │  面试服务    │
                 └──────┬──────┘   └──────┬──────┘   └──────┬──────┘
                        │                  │                  │
                        └──────────────────┼──────────────────┘
                                           │
                        ┌──────────────────┼──────────────────┐
                        │                  │                  │
                 ┌──────▼──────┐   ┌──────▼──────┐   ┌──────▼──────┐
                 │   MySQL     │   │   Redis     │   │ Elasticsearch│
                 └─────────────┘   └─────────────┘   └─────────────┘
```

### 6.3 安全设计

#### 认证与授权

```java
// JWT Token认证
@Configuration
public class SecurityConfig extends WebSecurityConfigurerAdapter {

    @Override
    protected void configure(HttpSecurity http) throws Exception {
        http
            .csrf().disable()
            .sessionManagement()
                .sessionCreationPolicy(SessionCreationPolicy.STATELESS)
            .and()
            .authorizeRequests()
                .antMatchers("/api/users/register", "/api/users/login").permitAll()
                .antMatchers("/api/admin/**").hasRole("ADMIN")
                .antMatchers("/api/teacher/**").hasAnyRole("TEACHER", "ADMIN")
                .anyRequest().authenticated()
            .and()
            .addFilterBefore(jwtAuthenticationFilter(),
                UsernamePasswordAuthenticationFilter.class);
    }
}
```

#### 数据安全

1. **密码加密**：BCrypt
2. **敏感数据加密**：AES-256
3. **SQL注入防护**：参数化查询
4. **XSS防护**：输入验证+输出编码
5. **CSRF防护**：Token验证

### 6.4 性能优化

#### 缓存策略

```java
// 多级缓存
@Service
public class QuestionService {

    @Cacheable(value = "questions", key = "#id")
    public Question getById(Long id) {
        return questionRepository.findById(id)
            .orElseThrow(() -> new NotFoundException("题目不存在"));
    }

    // 热门题目预加载
    @PostConstruct
    public void preloadHotQuestions() {
        List<Question> hotQuestions = questionRepository
            .findTop100ByOrderByViewCountDesc();

        for (Question q : hotQuestions) {
            redisTemplate.opsForValue()
                .set("question:" + q.getId(), q, 1, TimeUnit.HOURS);
        }
    }
}
```

#### 数据库优化

1. **索引优化**：常用查询字段建立索引
2. **读写分离**：主从复制
3. **分库分表**：按用户ID水平拆分
4. **连接池**：HikariCP
5. **慢查询优化**：定期分析优化

---

## 7. 实施计划

### 7.1 开发阶段

| 阶段 | 时间 | 内容 |
|------|------|------|
| 第一阶段 | 2周 | 用户模块、基础框架搭建 |
| 第二阶段 | 3周 | 知识库模块、题库模块 |
| 第三阶段 | 3周 | AI面试模块 |
| 第四阶段 | 2周 | 数据分析模块 |
| 第五阶段 | 2周 | 社区模块 |
| 第六阶段 | 2周 | 测试、优化、部署 |

### 7.2 技术难点

1. **AI面试对话质量**：需要精心设计prompt和评分算法
2. **能力模型准确性**：需要大量数据训练
3. **推荐算法效果**：需要持续优化
4. **高并发处理**：需要完善的缓存和队列机制

---

## 8. 总结

本系统以**智能化、个性化、数据化**为核心特色，通过：

1. **结构化知识体系** - 帮助用户系统学习
2. **智能题库系统** - 提供精准练习
3. **AI模拟面试** - 真实场景演练
4. **数据分析引擎** - 科学评估提升
5. **社区互动平台** - 分享交流成长

打造一个完整的就业面试辅助生态系统，助力求职者高效备战面试，成功获得理想Offer。
