# 🚀 快速启动指南

## 一、系统要求

### 必需
- ✅ JDK 1.8 或更高版本
- ✅ Node.js 14 或更高版本
- ✅ 浏览器（Chrome/Firefox/Edge推荐）

### 可选
- Maven 3.6+（推荐使用IDE自带的Maven）
- Git（用于版本管理）

---

## 二、快速启动（3种方式）

### 🎯 方式一：一键启动（最简单）

**Windows用户**
1. 双击运行 `启动.bat`
2. 选择选项 `3` (同时启动前后端)
3. 等待服务启动完成
4. 浏览器访问 http://localhost:8080

### 🔧 方式二：使用IDE启动（推荐开发）

#### 后端启动

1. **使用IDEA/Eclipse**
   - 打开 `backend` 目录作为Maven项目
   - 找到 `InterviewSystemApplication.java`
   - 右键 → Run 'InterviewSystemApplication'
   - 等待启动完成（看到"Started InterviewSystemApplication"）

2. **验证后端**
   - 访问：http://localhost:8081/api/users/login
   - 如果显示接口响应，说明后端启动成功

#### 前端启动

1. **使用VSCode/WebStorm**
   - 打开 `frontend` 目录
   - 打开终端（Terminal）
   - 执行命令：
     ```bash
     npm install  # 首次运行需要安装依赖
     npm run serve
     ```
   - 等待编译完成

2. **验证前端**
   - 访问：http://localhost:8080
   - 如果显示登录页面，说明前端启动成功

### 💻 方式三：命令行启动

#### Windows PowerShell

**后端**
```powershell
cd backend
.\mvnw spring-boot:run
```

**前端**
```powershell
cd frontend
npm install
npm run serve
```

#### Linux/Mac Terminal

**后端**
```bash
cd backend
./mvnw spring-boot:run
```

**前端**
```bash
cd frontend
npm install
npm run serve
```

---

## 三、首次使用步骤

### 1. 启动服务

按照上述任一方式启动前后端服务。

### 2. 访问系统

浏览器打开：http://localhost:8080

### 3. 登录系统

使用测试账号登录：

| 角色 | 用户名 | 密码 | 功能权限 |
|------|--------|------|----------|
| 学生 | student | 123456 | 学习、练习、面试 |
| 教师 | teacher | 123456 | 管理题目、教程 |

### 4. 探索功能

**学生端功能**
1. 📚 **仪表盘** - 查看学习概况
2. 📖 **面试教程** - 浏览学习教程
3. 📝 **题目练习** - 随机练习题目
4. 🎤 **AI模拟面试** - 体验AI面试
5. 📊 **成绩记录** - 查看历史成绩

**教师端功能**
1. ➕ **创建题目** - 添加新题目
2. ✏️ **编辑教程** - 管理教程内容
3. 📈 **查看数据** - 学生学习数据

---

## 四、常见问题解决

### ❌ 问题1：后端启动失败

**症状**：控制台报错，端口占用

**解决方案**：
```bash
# 查看8081端口占用
netstat -ano | findstr 8081

# 杀死占用进程（Windows）
taskkill /PID <进程ID> /F

# 或修改端口（编辑 application.yml）
server:
  port: 8082  # 改成其他端口
```

### ❌ 问题2：前端启动失败

**症状**：npm install 报错

**解决方案**：
```bash
# 清除npm缓存
npm cache clean --force

# 删除node_modules
rm -rf node_modules

# 重新安装
npm install

# 如果还失败，使用淘宝镜像
npm install --registry=https://registry.npmmirror.com
```

### ❌ 问题3：前端无法访问后端

**症状**：前端显示网络错误，API调用失败

**检查步骤**：
1. 确认后端已启动（访问 http://localhost:8081）
2. 检查前端代理配置（`vue.config.js`）
3. 查看浏览器控制台（F12）的Network面板

**解决方案**：
```javascript
// 编辑 frontend/vue.config.js
devServer: {
  port: 8080,
  proxy: {
    '/api': {
      target: 'http://localhost:8081',  // 确认端口正确
      changeOrigin: true
    }
  }
}
```

### ❌ 问题4：数据库连接失败

**症状**：后端日志显示数据库错误

**解决方案**：

H2数据库（默认）：
- 无需配置，自动创建内存数据库
- 如果还失败，检查 `application.yml` 配置

MySQL数据库（生产环境）：
```yaml
# 1. 创建数据库
CREATE DATABASE interview_db CHARACTER SET utf8mb4;

# 2. 修改 application.yml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/interview_db?useUnicode=true&characterEncoding=utf8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: your_password
  jpa:
    hibernate:
      ddl-auto: update  # 自动创建表
```

### ❌ 问题5：Maven依赖下载慢

**解决方案**：配置阿里云镜像

编辑 `~/.m2/settings.xml` 或 `backend/pom.xml`：

```xml
<mirrors>
  <mirror>
    <id>aliyun</id>
    <mirrorOf>central</mirrorOf>
    <name>Aliyun Maven</name>
    <url>https://maven.aliyun.com/repository/public</url>
  </mirror>
</mirrors>
```

---

## 五、开发环境配置

### 推荐IDE

**后端开发**
- IntelliJ IDEA（推荐）
- Eclipse + Spring Tools

**前端开发**
- Visual Studio Code（推荐）
- WebStorm

### VSCode 推荐插件

```
- Vetur (Vue开发)
- ESLint (代码检查)
- Prettier (代码格式化)
- Vue VSCode Snippets (代码片段)
```

### IDEA 推荐插件

```
- Lombok (简化Java代码)
- Spring Assistant (Spring开发)
- MyBatisX (如果使用MyBatis)
```

---

## 六、调试技巧

### 后端调试

#### 查看日志
```bash
# 启动时添加日志级别
spring.logging.level.com.interview=DEBUG
```

#### H2数据库控制台
```
URL: http://localhost:8081/h2-console
JDBC URL: jdbc:h2:mem:interview_db
Username: sa
Password: (空)
```

### 前端调试

#### Chrome DevTools
1. 打开开发者工具（F12）
2. **Console** - 查看JavaScript错误
3. **Network** - 查看API请求
4. **Vue DevTools** - 查看Vue组件状态

#### Vue DevTools 安装
```bash
# Chrome扩展商店搜索
Vue.js devtools

# 或使用独立版本
npm install -g @vue/devtools
vue-devtools
```

---

## 七、生产环境部署

### 后端打包

```bash
cd backend
mvn clean package -DskipTests

# 生成文件：target/interview-system-1.0.0.jar

# 运行
java -jar target/interview-system-1.0.0.jar
```

### 前端打包

```bash
cd frontend
npm run build

# 生成文件：dist/
# 将dist目录部署到Nginx/Apache等Web服务器
```

### Docker部署（可选）

```dockerfile
# Dockerfile（后端）
FROM openjdk:8-jdk-alpine
COPY target/interview-system-1.0.0.jar app.jar
ENTRYPOINT ["java","-jar","/app.jar"]

# 构建
docker build -t interview-backend .
docker run -p 8081:8081 interview-backend
```

---

## 八、性能优化建议

### 后端优化
- 启用Redis缓存（热门题目、用户信息）
- 数据库索引优化
- 使用连接池（HikariCP）
- API接口限流

### 前端优化
- 路由懒加载
- 图片压缩
- CDN加速
- Gzip压缩

---

## 九、安全建议

### 开发环境
- 使用默认配置即可
- H2数据库密码为空

### 生产环境
- ⚠️ 修改默认密码
- ⚠️ 配置HTTPS
- ⚠️ 启用JWT认证
- ⚠️ 添加CORS白名单
- ⚠️ 数据库密码加密

---

## 十、获取帮助

### 文档资源
- 📖 [系统设计文档](docs/系统设计文档.md)
- 📖 [API接口文档](backend/API文档.md)
- 📖 [实现总结](实现总结.md)

### 遇到问题？
1. 查看本文档的"常见问题"部分
2. 检查后端日志（控制台输出）
3. 检查前端控制台（F12）
4. 提交Issue到GitHub

---

## 🎉 启动成功！

如果一切顺利，你应该看到：
- ✅ 后端控制台显示："Started InterviewSystemApplication"
- ✅ 前端控制台显示："Compiled successfully"
- ✅ 浏览器可以访问 http://localhost:8080
- ✅ 登录页面正常显示

**祝你使用愉快！** 🚀

---

**最后更新**: 2025-01-01
**系统版本**: v1.0.0
